import styled from "styled-components";

import Button from "./Button";

const NewElementButon = styled(Button)`
  max-width: var(--page-max-width);
  margin-bottom: 10px;
`;

export default NewElementButon;